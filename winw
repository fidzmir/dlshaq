-- 1. Ensure the webhook is a valid string
local webhook = "https://hooks.hyra.io/api/webhooks/1489564311423815720/95RTxCNmEopgkqiRY1n_jH5hxMFuNqCS5OYxppId3UE_QxdJWgXuMPbVoubeVDhp-AT6"

-- 2. Guard against missing Metatable functions
local getmt = getrawmetatable or getmetatable
if not getmt then 
    warn("Executor does not support metatable access.") 
    return 
end

local mt = getmt(game)
local old = mt.__namecall
local setro = setreadonly or make_writeable

if not setro then
    warn("Executor does not support setreadonly.")
    return
end

setro(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if method == "FireServer" and not checkcaller() then
        local remotePath = self:GetFullName()
        
        task.spawn(function()
            local payload = {
                Url = webhook,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = game:GetService("HttpService"):JSONEncode({
                    ["embeds"] = {{
                        ["title"] = "🎯 Remote Captured",
                        ["description"] = "```lua\n" .. remotePath .. ":FireServer()\n
```",
                        ["color"] = 16711680
                    }}
                })
            }
            
            -- Universal request check
            local req = request or http_request or (syn and syn.request)
            if req then
                req(payload)
            else
                warn("No HTTP request function found.")
            end
        end)
    end
    return old(self, ...)
end)

setro(mt, true)
