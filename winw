task.wait(1)

local WebhookURL = "https://hooks.hyra.io/api/webhooks/1489564311423815720/95RTxCNmEopgkqiRY1n_jH5hxMFuNqCS5OYxppId3UE_QxdJWgXuMPbVoubeVDhp-AT6"

-- Verify environment functions exist
local gmt = getrawmetatable or getmetatable
local sro = setreadonly or make_writeable
local req = request or http_request or (http and http.request)

if not gmt or not sro then
    print("Delta Error: Metatable functions not supported in this version.")
    return
end

local mt = gmt(game)
local oldNamecall = mt.__namecall

sro(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    -- Filter: Only Client-to-Server and not internal calls
    if method == "FireServer" and not checkcaller() then
        local name = self.Name
        
        task.spawn(function()
            if req then
                local data = {
                    Url = WebhookURL,
                    Method = "POST",
                    Headers = {["Content-Type"] = "application/json"},
                    Body = game:GetService("HttpService"):JSONEncode({
                        embeds = {{
                            title = "📤 Outgoing Remote",
                            description = "Remote Name: `" .. name .. "`",
                            color = 0x00FF00
                        }}
                    })
                }
                req(data)
            end
        end)
    end
    
    return oldNamecall(self, ...)
end)

sro(mt, true)
print("Spy Active - Monitoring Outgoing Remotes")
