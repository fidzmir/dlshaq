local webhook = "https://discord.com/api/webhooks/1489564311423815720/95RTxCNmEopgkqiRY1n_jH5hxMFuNqCS5OYxppId3UE_QxdJWgXuMPbVoubeVDhp-AT6"



local r = request or http_request or (http and http.request)

local m = getrawmetatable(game)

local o = m.__namecall

setreadonly(m, false)



m.__namecall = newcclosure(function(self, ...)

    local method = getnamecallmethod()

    if method == "FireServer" then 

        print("FOUND REMOTE: " .. self.Name) -- This prints in Delta console

        

        task.spawn(function()

            if r then

                local res = r({

                    Url = webhook,

                    Method = "POST",

                    Headers = {["Content-Type"] = "application/json"},

                    Body = game:GetService("HttpService"):JSONEncode({

                        content = "Captured: " .. self.Name

                    })

                })

                print("Webhook Status: " .. tostring(res.StatusCode))

            end

        end)

    end

    return o(self, ...)

end)

setreadonly(m, true)

print("Spy is running! Watch the console.")
